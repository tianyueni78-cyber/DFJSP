# 阶段 A 第 13 步：完全重调度搜索与组合评价

## 1. 目标

在第 12 步正式冻结问题上，对 `53` 道未开工工序运行同等预算的完全重调度
搜索，再与 AGV 联动部分右移方案统一计算 `tD`、`SD` 和 `Y`。

## 2. 正式输入

```text
正常基线完工时间：112.72
故障：J8-O1 / M2 / tf=12 / tr=5
冻结工序：5
可重调度工序：53
```

## 3. 搜索预算

与第 10 步正常基线搜索一致：

- 种群 `10`；
- 最大 `100` 代；
- 交叉概率 `0.8`；
- 变异概率 `0.2`；
- 连续 `10` 代无 Pareto 改善停止；
- 最长 `30` 秒；
- 随机种子 `42`。

## 4. 评价与选择

完全重调度目标：

```text
最终卸载最大完工时间
机器与 AGV 总能耗
```

组合指标：

```text
tD = 候选最终卸载完工时间 - 正常基线完工时间
SD = 未开工工序机器分配变化数
Y = 0.9 * tD + 0.1 * SD
```

候选包括一个 AGV 联动部分右移方案和全部去重完全重调度 Pareto 方案。

## 5. 代码修改

新增：

- `configs/stage_a_step_13_config.m`
- `scripts/run_stage_a_step_13_contract.m`
- `scripts/run_stage_a_step_13_search_and_selection.m`
- `tests/test_stage_a_step_13_contract.m`

不修改完全重调度搜索、候选解码、能耗计算和组合评价核心算法。

## 6. 测试与正式运行

轻量契约测试：

```matlab
run(fullfile(pwd, 'tests', ...
    'test_stage_a_step_13_contract.m'))
```

正式运行会生成新输出目录：

```matlab
stage13 = run_stage_a_step_13_search_and_selection(stage12);
```

输出包括：

- `result.mat`
- `pareto_objectives.csv`
- `search_history.csv`
- `combination_evaluations.csv`
- `run_summary.txt`

## 7. 路径依赖修正

首次契约测试暴露出第 13 步入口遗漏 `src/scheduling/` 路径，导致完全重调度
解码器无法找到原运输时间函数：

```text
spare_transfer_time_compute
```

现已在轻量入口和正式入口中统一加入 `src/scheduling/`，并在契约测试中
检查依赖文件存在。入口内部在搜索前检查函数可见，入口结束后恢复原 MATLAB
路径，因此测试不再错误要求该函数在入口返回后仍永久可见。没有修改运输
时间函数或解码算法。

## 8. 契约测试结果

MATLAB 轻量契约测试已通过：

```text
test_stage_a_step_13_contract passed
```

测试内部使用 `144.2033 / J5-O1` 的 `contractScenario`，执行 `6` 个体、
`2` 代搜索，只验证：

- 完全重调度搜索可以运行；
- Pareto 目标去重有效；
- 部分右移与完全重调度候选可以统一计算 `tD`、`SD`、`Y`；
- 第 13 步搜索预算配置与第 10 步一致；
- 契约运行不生成正式输出目录。

测试输出不是正式研究结果。正式运行必须传入第 12 步的优化基线场景。

## 9. 正式运行结果

结果目录：

```text
outputs/stage_a_step_13_search_and_selection/20260610_135428/
```

搜索概况：

| 项目 | 结果 |
|---|---:|
| 停止原因 | `no_pareto_improvement` |
| 实际完成代数 | `97` |
| 运行时间 | `16.312482` 秒 |
| 去重 Pareto 候选 | `4` |

完全重调度 Pareto 目标：

| 候选 | 最终卸载完工时间 | 总能耗 |
|---:|---:|---:|
| 1 | `111.14333333` | `1766.41566667` |
| 2 | `108.65333333` | `1787.37933333` |
| 3 | `108.90666667` | `1767.32666667` |
| 4 | `111.11666667` | `1766.997` |

组合评价：

| 策略 | 最终卸载完工时间 | `tD` | `SD` | `Y` |
|---|---:|---:|---:|---:|
| 部分右移 | `116.72` | `4` | `0` | `3.6` |
| 完全重调度候选 1 | `111.14333333` | `-1.57666667` | `30` | `1.581` |
| 完全重调度候选 2 | `108.65333333` | `-4.06666667` | `30` | `-0.66` |
| 完全重调度候选 3 | `108.90666667` | `-3.81333333` | `30` | `-0.432` |
| 完全重调度候选 4 | `111.11666667` | `-1.60333333` | `30` | `1.557` |

在 `omega1=0.9、omega2=0.1` 下，选择完全重调度候选 2。

## 10. 结论边界

本次结果说明：

- 部分右移保持机器分配稳定，但最终卸载比正常基线晚 `4`；
- 完全重调度通过改变 `30` 道未开工工序的机器分配，将最终卸载提前约 `4.07`；
- 当前权重更重视完工时间，因此完全重调度的综合指标更优。

该结果只对应随机种子 `42`、单个故障场景和权重 `0.9/0.1`。仍需多随机
种子和权重敏感性分析，才能判断选择是否稳定。

## 11. 下一步

阶段 A 第 14 步：重复随机种子搜索、权重敏感性分析，并对最终候选执行
机器、工艺、AGV、维修区间、最终卸载和能耗约束审计。
