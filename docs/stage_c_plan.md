# 阶段 C：多机器故障详细计划

## 1. 阶段目标

在阶段 A、B 和 B-R 已验证的单机器故障链路上，建立支持多个机器故障事件
的 FJSP-AGV 动态重调度程序，分别解决：

1. 多台机器在同一时刻同时故障；
2. 多个机器故障按时间连续发生；
3. 多故障下的局部右移、完全重调度、AGV 联动和组合选择。

阶段 C 不修改 `raw_code/`，继续使用原项目数据。故障场景只改变实验参数，
不自行生成新的生产问题数据。

## 2. 核心原则

### 2.1 同时故障不是多个单故障结果简单相加

同一工序可能同时位于多个影响传播链中。程序必须合并影响集合，并以全部
前序、机器和运输约束共同决定其最早可执行时间。

### 2.2 连续故障不能回到原始正常基线

第二次故障必须从第一次重调度后已经执行到该时刻的计划中提取状态。已经
完成的加工和运输继续冻结，不能因后续故障被撤销。

### 2.3 中断规则按事件显式记录

加工中故障沿用已验证规则：

- `resume_remaining`：保留进度，修复后继续剩余时间；
- `restart_from_zero`：进度作废，修复后从头加工。

第一轮同时故障先使用同一种规则验证多故障主链。主链稳定后，再允许不同
故障事件使用不同中断规则，避免同时引入过多变量。

## 3. 统一事件模型

阶段 C 使用故障结构体数组：

```matlab
faults(k).event_id
faults(k).machine_id
faults(k).start_time
faults(k).repair_duration
faults(k).repair_end_time
faults(k).interruption_rule
```

规范化后还应记录：

```matlab
faults(k).event_group
faults(k).source_order
```

- `event_group` 标识相同故障时刻的一组同时故障；
- `source_order` 保留输入顺序，用于结果追踪；
- 程序按 `start_time` 排序；
- 同一机器上重叠或相接的维修区间合并；
- 不同机器的维修区间分别保存，不能错误合并。

## 4. 阶段结构

阶段 C 共分为四个工作段、18 个步骤。

### C-0：共享多故障基础

#### 第 1 步：定义并校验 `faults[]`

**实现状态：代码、轻量测试和说明文档已完成，MATLAB 轻量测试已通过。**

工作：

- 建立统一多故障事件结构；
- 校验机器编号、故障时间、维修时长和中断规则；
- 按时间稳定排序；
- 给同时故障分配相同 `event_group`。

完成标准：

- 单故障、同时故障和连续故障均可使用同一输入接口；
- 非法事件能给出明确错误；
- 排序不丢失原事件身份。

详细记录：

- [阶段 C 第 1 步：统一多故障事件输入](stage_c_step_01_fault_events.md)

#### 第 2 步：生成每台机器的不可用区间

**实现状态：代码、轻量测试和说明文档已完成，MATLAB 轻量测试已通过。**

工作：

- 按机器汇总维修区间；
- 合并同一机器上重叠或相接的区间；
- 保留区间对应的原故障事件编号。

完成标准：

- 任意机器可有零个、一个或多个不可用区间；
- 合并后区间有序且不重叠；
- 不同机器的区间互不混淆。

详细记录：

- [阶段 C 第 2 步：机器维修不可用区间](stage_c_step_02_machine_unavailability.md)

#### 第 3 步：建立多故障时刻状态提取器

**实现状态：代码、轻量测试和说明文档已完成，MATLAB 轻量测试已通过。**

工作：

- 对一个事件组同时提取已完成、正常在制、故障在制和未开工任务；
- 识别每台故障机器上的直接中断工序；
- 识别故障时刻已完成和正在执行的 AGV 任务。

完成标准：

- 每道工序恰好属于一个状态；
- 每个直接中断工序只关联其所在故障机器；
- 状态快照不修改输入计划。

详细记录：

- [阶段 C 第 3 步：多故障事件组状态快照](stage_c_step_03_event_group_state.md)

### C-S：多机器同时故障

#### 第 4 步：构建同时故障实验场景

**实现状态：配置、筛选、场景入口、轻量测试和说明文档已完成，MATLAB
轻量测试已通过。**

工作：

- 从原数据基线动态筛选至少两台有实际影响的机器；
- 使用同一故障时刻建立同时故障事件组；
- 第一版统一使用一种中断规则。

完成标准：

- 至少两个故障事件均直接影响工序或后续机器队列；
- 场景来源、筛选理由和维修参数可追踪；
- 不新增生产数据。

详细记录：

- [阶段 C 第 4 步：同时故障场景筛选](stage_c_step_04_simultaneous_fault_scenario.md)

#### 第 5 步：分别传播并合并影响集合

**实现状态：双根传播、集合合并、轻量入口、测试和说明文档已完成，
MATLAB 轻量测试已通过。**

工作：

- 为每个故障根建立工件后继和机器后继传播；
- 合并重复工序；
- 对同一工序保留全部影响来源；
- 使用最大约束到达时间决定延迟。

完成标准：

- 合并集合无重复、无遗漏；
- 结果与故障输入顺序无关；
- 每个受影响工序可追踪到一个或多个故障源。

详细记录：

- [阶段 C 第 5 步：同时故障影响传播与合并](stage_c_step_05_simultaneous_impact.md)

#### 第 6 步：生成同时故障机器局部右移方案

**实现状态：机器候选表、双中断恢复承诺、轻量入口、测试和说明文档已完成，
MATLAB 轻量测试已通过。**

工作：

- 写入多个维修不可用区间；
- 处理多个中断工序承诺；
- 重建受影响机器时间表；
- 保持未受影响工序及机器分配不变。

完成标准：

- 所有维修区间内无加工；
- 机器和工件时间不冲突；
- 每个中断工序满足指定恢复规则；
- 未受影响任务保持原计划。

详细记录：

- [阶段 C 第 6 步：同时故障机器局部右移](stage_c_step_06_simultaneous_machine_right_shift.md)

#### 第 7 步：分析同时故障对 AGV 的影响

**实现状态：直接失效识别、同 AGV 后继传播、来源保留、轻量入口、测试和
说明文档已完成，MATLAB 轻量测试已通过。**

工作：

- 检查运输到达与新机器开工时间；
- 检查多个故障传播链对同一运输的共同影响；
- 沿同一 AGV 后续任务传播待复核范围。

完成标准：

- 直接失效运输和传播复核运输均被识别；
- 同一运输任务不重复加入；
- 本步只分析，不修改时间表。

详细记录：

- [阶段 C 第 7 步：同时故障 AGV 影响分析](stage_c_step_07_simultaneous_agv_impact.md)

#### 第 8 步：完成 AGV 与机器联动局部右移

**实现状态：AGV 与机器联动传播、双中断承诺保护、多维修区间校验、轻量
入口、测试和说明文档已完成，MATLAB 轻量测试已通过。**

工作：

- 正式调整受影响运输；
- 避免同一 AGV 时间重叠；
- 将运输延迟反馈给目标工序；
- 必要时再次传播机器和工件后续影响。

完成标准：

- 加工不早于工件到达；
- AGV 活动无重叠；
- 所有工件完成最终卸载；
- 多维修区间和中断承诺全部通过验证。

详细记录：

- [阶段 C 第 8 步：同时故障 AGV 与机器联动局部右移](stage_c_step_08_simultaneous_agv_linked_right_shift.md)

#### 第 9 步：建立同时故障完全重调度冻结问题

**实现状态：冻结与释放分区、多中断承诺、维修和资源边界、轻量入口、测试
和说明文档已完成，MATLAB 轻量测试已通过。**

工作：

- 冻结故障时刻前已执行任务；
- 冻结正常在制任务和各中断工序承诺；
- 建立多个机器不可用区间和资源释放边界；
- 提取可重新调度的未开工工序。

完成标准：

- 冻结任务与可调度任务不重叠；
- 所有未开工工序完整进入决策集合；
- 多个中断承诺和维修区间完整保存。

详细记录：

- [阶段 C 第 9 步：同时故障完全重调度冻结问题](stage_c_step_09_simultaneous_frozen_problem.md)

#### 第 10 步：实现同时故障完全重调度解码与搜索

**当前状态：第 10.1 至第 10.3 步轻量测试已通过；第 10.4 步正式配置、
结果保存入口、配置测试和说明文档已完成静态实现，尚未运行 MATLAB，尚未
生成正式输出。**

工作：

- 扩展解码器支持多个维修区间和中断承诺；
- 复用 OS、MS、AS、SS 编码和受限 NSGA-II；
- 保留 Pareto 去重和自适应停止。

完成标准：

- 每个候选工序恰好调度一次；
- 所有候选通过机器、工件、AGV、维修和中断约束；
- 轻量搜索可复现；
- 正式搜索入口与轻量测试分离。

实施拆分：

1. `10.1`：多中断承诺和多维修区间的单候选解码；
2. `10.2`：受限种群初始化、交叉和变异；
3. `10.3`：候选评价与受限 NSGA-II 轻量搜索；
4. `10.4`：正式搜索配置和结果保存入口。

详细记录：

- [阶段 C 第 10.1 步：同时故障完全重调度解码器](stage_c_step_10a_simultaneous_complete_decoder.md)
- [阶段 C 第 10.2 步：同时故障受限种群与遗传算子](stage_c_step_10b_simultaneous_reschedule_operators.md)
- [阶段 C 第 10.3 步：候选评价与受限 NSGA-II 轻量搜索](stage_c_step_10c_simultaneous_restricted_search.md)
- [阶段 C 第 10.4 步：正式搜索配置与结果保存入口](stage_c_step_10d_formal_search_entry.md)

#### 第 11 步：同时故障组合评价与审计

**当前状态：评价、局部右移能耗、组合选择、多约束审计、轻量入口、测试和
说明文档已完成；MATLAB 轻量测试和正式结果分析均已通过。**

工作：

- 计算局部右移和完全重调度的 `tD、SD、Y`；
- 计算机器和 AGV 能耗；
- 执行权重敏感性与最终约束审计。

完成标准：

- 两类候选使用同一故障前基线；
- 能输出分项指标和最终策略；
- 所有约束与能耗审计通过。

详细记录：

- [阶段 C 第 11 步：组合评价与最终审计](stage_c_step_11_combination_and_audit.md)

### C-SEQ：多个故障连续发生

#### 第 12 步：建立事件回放与计划版本模型

**当前状态：基线初始化、版本追加、按时刻解析、轻量入口、测试和说明文档
已完成静态实现，尚未运行 MATLAB。**

工作：

- 按故障时间依次处理事件组；
- 为正常基线和每轮重调度结果分配计划版本；
- 记录每个版本的生效时刻和来源事件。

完成标准：

- 可追踪 `baseline -> event 1 -> event 2 -> ...`；
- 后续事件只读取当时有效计划；
- 不覆盖历史版本。

详细记录：

- [阶段 C 第 12 步：事件回放与计划版本模型](stage_c_step_12_plan_version_history.md)

#### 第 13 步：从当前计划提取下一故障状态

**当前状态：当前计划视图、下一故障动态筛选、连续故障状态提取、旧维修保留、
轻量入口、测试和说明文档均已完成，MATLAB 契约测试已通过。**

工作：

- 在下一故障时刻截取上一版本计划；
- 冻结该时刻前已完成的加工和运输；
- 识别新故障直接中断工序；
- 保留尚未结束的既有维修区间。

完成标准：

- 已执行历史不会回滚；
- 上一轮重调度后已经改变的机器和 AGV 分配被正确继承；
- 旧故障和新故障可同时处于维修状态。

详细记录：

- [阶段 C 第 13 步：从当前计划提取下一故障状态](stage_c_step_13_next_fault_state.md)

#### 第 14 步：处理连续故障影响集合与累计维修区间

**当前状态：累计维修区间、新故障影响传播、零影响下一故障重筛、活动历史影响过滤、
集合合并、轻量入口、测试和说明文档已完成静态实现，等待 MATLAB 契约测试。**

工作：

- 合并历史未结束影响和新故障影响；
- 处理同一工序被先后多个故障影响；
- 更新每台机器累计不可用区间。

完成标准：

- 无重复调度或重复中断承诺；
- 新计划满足全部尚未结束的维修区间；
- 影响来源和事件轮次可追踪。

详细记录：

- [阶段 C 第 14 步：累计维修区间与连续故障影响合并](stage_c_step_14_sequential_impact_context.md)

#### 第 15 步：连续执行局部右移与 AGV 联动

**当前状态：连续故障机器局部右移、AGV 影响分析、AGV 联动调整、
轻量入口、测试和说明文档已完成静态实现，等待 MATLAB 契约测试。**

工作：

- 从当前计划而非原基线生成局部修复；
- 逐轮调整机器和 AGV 时间；
- 保存每轮候选和验证结果。

完成标准：

- 每轮均生成可行局部方案；
- 每轮已冻结历史保持不变；
- 最终计划通过全时间轴约束检查。

代码入口：

- [阶段 C 第 15 步：连续故障局部右移与 AGV 联动](stage_c_step_15_sequential_agv_linked_right_shift.md)

#### 第 16 步：连续执行完全重调度与组合选择

**当前状态：第 16.1 步连续故障完全重调度冻结问题已通过；第 16.2 步
连续故障完全重调度解码器已通过；第 16.3 步连续故障算子与轻量搜索契约
已通过；第 16.4 步连续故障正式搜索已完成；第 16.5 步连续故障组合评价
契约测试和正式结果分析已通过；第 16.6 步结果总结已完成。**

工作：

- 每轮重建冻结问题；
- 对当时未开工工序重新搜索；
- 每轮计算 `tD、SD、Y` 并选择生效方案。

代码入口：

- [阶段 C 第 16.1 步：连续故障完全重调度冻结问题](stage_c_step_16_1_sequential_frozen_problem.md)
- [阶段 C 第 16.2 步：连续故障完全重调度解码器](stage_c_step_16_2_sequential_complete_decode.md)
- [阶段 C 第 16.3 步：连续故障算子与轻量搜索契约](stage_c_step_16_3_sequential_search_contract.md)
- [阶段 C 第 16.4 步：连续故障正式搜索入口](stage_c_step_16_4_sequential_formal_search_entry.md)
- [阶段 C 第 16.5 步：连续故障组合评价](stage_c_step_16_5_sequential_combination_selection.md)
- [阶段 C 第 16.6 步：连续故障正式结果总结](stage_c_step_16_6_sequential_result_summary.md)

完成标准：

- 每轮搜索只改变当时尚未开工任务；
- 每轮评价基准和比较口径明确；
- 选定方案成为下一事件的输入计划。

正式结果摘要：

- 输出目录：`outputs/stage_c_sequential_complete_reschedule_search/20260616_092142`
- 正式搜索停止原因：`maximum_generations`
- Pareto 解数量：`2`
- 连续故障最终选择：`complete_rescheduling`
- 选定方案：`tD = -27.7133`，`SD = 13`，`Y = -23.6420`
- 局部右移对比：`tD = 3`，`SD = 0`，`Y = 2.7000`
- 约束审计和能耗审计均通过。

### C-V：最终验证与收口

#### 第 17 步：场景矩阵、随机种子与最终审计

**当前状态：第 17.1 步最终审计场景矩阵配置已通过；第 17.2 步最终审计
多随机种子入口、配置测试和正式五随机种子实验均已完成；第 18 步阶段 C
总结与代码导读已完成静态整理。**

至少验证：

| 场景 | 故障关系 | 中断规则 |
|---|---|---|
| C-S1 | 两台机器同时故障 | 统一保留进度 |
| C-S2 | 两台机器同时故障 | 统一从头加工 |
| C-SEQ1 | 两次故障先后发生且维修不重叠 | 统一规则 |
| C-SEQ2 | 后一故障发生时前一机器仍在维修 | 统一规则 |

完成标准：

- 每个场景均通过约束和能耗审计；
- 正式搜索执行多随机种子验证；
- 记录策略稳定性和解质量波动；
- 不根据单个种子下绝对结论。

详细记录：

- [阶段 C 第 17.1 步：最终审计场景矩阵](stage_c_step_17_1_final_audit_matrix.md)
- [阶段 C 第 17.2 步：最终审计多随机种子入口](stage_c_step_17_2_final_audit_multiseed_entry.md)

正式结果摘要：

- 输出目录：`outputs/stage_c_final_audit_multiseed/20260616_094301`
- 覆盖场景：`C-S1` 和 `C-SEQ1`
- 总运行次数：`10`
- 全部约束与能耗审计通过；
- `10` 次运行全部选择 `complete_rescheduling`；
- `C-S1` 最优 `Y=-28.9830`，平均 `Y=-20.7118`；
- `C-SEQ1` 最优 `Y=-23.6420`，平均 `Y=-17.8016`；
- `C-S2` 和 `C-SEQ2` 尚未实现，不纳入本次正式结论。

#### 待补场景 C-S2：同时故障下从头加工

研究问题：

> 当多台机器同时故障，且故障前已加工进度全部作废、修复后必须从头加工时，
> 当前 Stage C 同时故障重调度框架是否仍然可行？从头加工会如何影响最终
> 卸载时间、能耗、机器分配扰动和最终策略选择？

工作步骤：

1. 定义同时故障下的从头加工中断承诺；
2. 为每个中断工序记录故障前损失加工段和修复后完整重加工段；
3. 扩展同时故障局部右移，使其写入完整重加工段；
4. 扩展 AGV 影响分析和 AGV 联动右移；
5. 扩展完全重调度冻结问题和解码器；
6. 运行轻量搜索契约；
7. 建立正式搜索、组合选择和多随机种子入口；
8. 与 `C-S1` 保留进度场景对比。

完成标准：

- `restart_from_zero=true`；
- `progress_preserved=false`；
- 损失加工和完整重加工均计入机器工作能耗；
- 维修停机区间不计入机器工作能耗；
- 局部右移和完全重调度均通过机器、工件、AGV、维修、最终卸载和能耗审计；
- 能输出与 `C-S1` 可比的最终卸载、`tD`、`SD`、`Y` 和总能耗。

#### 待补场景 C-SEQ2：连续故障且维修区间重叠

研究问题：

> 当后一故障发生时，前一台机器仍处于维修不可用状态，当前连续故障计划
> 版本机制是否仍能正确保留历史维修约束，并在新故障下生成可行重调度方案？

工作步骤：

1. 构造维修区间重叠的连续故障事件；
2. 从当前生效计划提取下一故障状态；
3. 保留仍未结束的历史维修区间；
4. 合并历史维修区间与新维修区间；
5. 传播新故障影响并保留历史影响；
6. 生成局部右移和 AGV 联动候选；
7. 建立完全重调度冻结问题；
8. 运行轻量搜索、正式搜索、组合选择和多随机种子审计。

完成标准：

- 后一故障处理时不回滚前一故障历史；
- 当前计划视图包含仍有效的历史维修约束；
- 同一机器维修区间重叠或相接时规范化正确；
- 不同机器维修区间互不混淆；
- 最终候选通过全时间轴机器、工件、AGV、维修和能耗审计；
- 能输出与 `C-SEQ1` 可比的最终卸载、`tD`、`SD`、`Y` 和总能耗。

#### 第 18 步：阶段 C 总结与代码导读

工作：

- 汇总数学模型、事件模型、编码、解码、搜索和测试；
- 记录同时故障与连续故障的结果和限制；
- 为新增核心代码添加分块注释和入口链接。

完成标准：

- 能从 README 进入阶段 C 计划、代码、测试和最终结果；
- 阶段 C 的能力边界与未解决问题明确；
- 文档结果与正式输出一致。

详细记录：

- [阶段 C 最终总结与代码导读](stage_c_final_summary_and_code_guide.md)
- [项目总收口](project_final_summary.md)

## 5. 整体完成标准

阶段 C 只有同时满足以下条件才算完成：

1. 同一 `faults[]` 接口支持单故障、同时故障和连续故障；
2. 同一机器多个维修区间正确规范化；
3. 多影响集合合并后无重复、无遗漏；
4. 同时故障可生成局部右移和完全重调度方案；
5. 连续故障从当前生效计划继续，不回滚历史；
6. 机器、工件、AGV、维修、中断和最终卸载约束全部通过；
7. 能源计算覆盖损失加工、有效加工、运输和充电；
8. 每轮可计算 `tD、SD、Y` 并执行组合选择；
9. 场景矩阵和多随机种子实验完成；
10. 最终总结、工作流程和代码入口全部更新。

## 6. 实施顺序

严格按第 1 至第 18 步推进，每次只实现并测试一个小步骤。前一步轻量契约
未通过，不进入下一步。正式搜索、多随机种子实验和输出生成继续在运行前
单独确认。

阶段 C 第 1 步只定义和校验 `faults[]`，不修改现有 A/B/B-R 算法，不运行
MATLAB。
