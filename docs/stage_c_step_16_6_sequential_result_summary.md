# 阶段 C 第 16.6 步：连续故障正式结果总结

## 结果来源

- 正式搜索输出目录：`outputs/stage_c_sequential_complete_reschedule_search/20260616_092142`
- 搜索结果文件：`result.mat`
- Pareto 目标：`pareto_objectives.csv`
- 搜索历史：`search_history.csv`
- 运行摘要：`run_summary.txt`

本总结基于已完成的正式搜索和组合评价，不重新运行 MATLAB。

## 正式搜索配置

- 种群规模：`10`
- 最大代数：`100`
- 连续无改善停止：`10` 代
- 时间上限：`30` 秒
- 随机种子：`42`

## 正式搜索结果

- 停止原因：`maximum_generations`
- 实际运行代数：`100`
- 运行时间：`27.5114` 秒
- 去重后 Pareto 解数量：`2`

Pareto 候选：

| 候选 | 最终卸载完工时间 | 总能耗 |
|---:|---:|---:|
| 1 | `117.5` | `1815.2` |
| 2 | `113.5` | `1817.8` |

## 组合评价结果

组合评价使用当前计划 `V1` 的完工时间作为比较基线：

- 当前计划基线完工时间：`141.2033`
- 权重：`omega1 = 0.9`，`omega2 = 0.1`
- 选择结果：`complete_rescheduling`

选定完全重调度方案：

| 指标 | 数值 |
|---|---:|
| 候选完工时间 | `113.4900` |
| `tD` | `-27.7133` |
| `SD` | `13` |
| `Y` | `-23.6420` |
| 可重调度工序数 | `38` |

局部右移对比：

| 指标 | 数值 |
|---|---:|
| 候选完工时间 | `144.2033` |
| `tD` | `3` |
| `SD` | `0` |
| `Y` | `2.7000` |
| 可重调度工序数 | `38` |

## 审计结论

- 约束审计：通过
- 能耗审计：通过
- 维修区间审计：通过
- 中断承诺审计：通过
- 最终卸载审计：通过

## 当前结论

在当前单随机种子、当前权重配置下，连续故障场景中完全重调度优于局部右移。
它牺牲了 `13` 个未开工工序的机器分配稳定性，但显著降低最终卸载完工时间，
使组合指标 `Y` 更低。

## 限制

该结论来自一个随机种子和一个连续故障场景。它可以作为阶段 C 连续故障流程
打通的正式样例，但不能替代第 17 步中的场景矩阵和多随机种子稳定性验证。
